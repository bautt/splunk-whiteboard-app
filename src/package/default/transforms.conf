[whiteboards]
external_type = kvstore
collection = whiteboards
fields_list = _key, name, tags, owner, updated_at, elements_json

[whiteboard_versions]
external_type = kvstore
collection = whiteboard_versions
fields_list = _key, board_id, label, snapshot_json, created_at
